# Дорожная карта Spectemus Simul

## Зачем существует этот документ

Это единый порядок развития продукта. Новая продуктовая ветка начинается только
с задачи из этой карты или после её явного обновления. Так мы не добавляем
случайные функции и не ломаем главную идею: небольшой приватный просмотр своего
фильма вместе с близкими людьми.

## Неподвижные принципы

- **Главный сценарий:** host выбирает фильм на своём компьютере, создаёт
  приватную комнату и отправляет гостю понятное приглашение.
- **Фильм не становится загрузкой в сервис:** приложение не хранит оригинал,
  путь к нему или каталог фильмов. В Internet mode между участниками идёт
  защищённый медиапоток, а не облачная копия файла.
- **LAN и Internet — разные режимы:** LAN остаётся простым и бесплатным,
  Internet mode добавляет account, отзывное приглашение, HTTPS и TURN. Новый
  режим не ослабляет безопасность существующего LAN-приглашения.
- **Не строим:** torrent/URL ingest, обход DRM, видеокаталог, запись просмотра,
  массовые комнаты и монетизацию до подтверждённой ценности и стоимости.
- **Сначала надёжность, затем расширение:** у каждой фазы есть проверяемый
  выход; «код написан» не равен «функция готова для людей».

## Текущее состояние

LAN desktop preview уже умеет создать комнату, поделиться ссылкой/QR,
синхронно передавать видео и звук, чат и восстановление сессии. WT-682 добавил
локальную подготовку распространённых контейнеров в временный H.264/AAC MP4.
Готовые installers проходят CI install smoke на Apple Silicon, Intel Mac и
Windows.

Это ещё не обещание «любой фильм работает». Перед следующим публичным заявлением
нужны реальные прогоны Mac ↔ Windows с MP4, MKV H.264/AAC, MKV HEVC/AAC и MKV
H.264/DTS: изображение, звук, пауза, перемотка и не менее десяти минут показа.

## Порядок задач

| Фаза | Задача                                   | Результат                                                                                       | Gate перехода                                                                                            |
| ---- | ---------------------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 0    | WT-682 local media normalizer            | Надёжная локальная подготовка видео без изменения оригинала                                     | Реальная Mac ↔ Windows matrix на свежих installers; нет регрессии MP4/WebM                               |
| 1    | WT-654 passwordless accounts and invites | Изолированный `/api/v2`: account, одноразовый email challenge, membership, отзыв invite и audit | Выбран PostgreSQL migration path и email provider; expired/revoked invite не даёт access или media token |
| 2    | WT-655 internet room runtime             | Отдельный выбор Internet room, краткоживущий LiveKit token, revoke/disconnect, диагностика сети | Staging VM, DNS, HTTPS/WSS, TURN и firewall прошли проверку из WT-680/WT-610                             |
| 3    | WT-656 private Internet alpha            | Ограниченная invite-only проверка «host дома → гость в другом городе»                           | Сессии из разных сетей, включая сложную сеть; измерены QoS, TURN usage, стоимость и support load         |
| 4    | Closed beta → release                    | Понятная установка, операционный контур и честный UX для малых private rooms                    | Успешность сессий, privacy/deletion policy, incident/revoke runbook и проверенная экономика              |

## Что делаем следующим

Следующая ветка после закрытия физических LAN-прогонов — **WT-654**. Она не
открывает сервис в интернет и не меняет `/api/v1` LAN rooms. Её задача —
реализовать public access boundary, описанную в
[WT-652](WT-652_INTERNET_MODE_ARCHITECTURE.md) и
[WT-653](WT-653_PUBLIC_ACCESS_CONTRACTS.md).

До начала реальной отправки писем и внешнего deploy понадобятся решения,
которые нельзя выдумать в коде:

1. PostgreSQL для persistent accounts/invites: локальная разработка и
   последующий staging путь.
2. Почтовый provider и подтверждённый sender domain для одноразовых кодов.
3. VM, три DNS-имени (`app`, `rtc`, `turn`) и доступ администратора — только
   перед WT-655/staging, не раньше.

## Рабочее правило для каждой задачи

1. Перед веткой фиксируем одну цель, границу и критерии готовности в документе
   WT-задачи.
2. Меняем минимально необходимый слой, не смешивая LAN и public mode.
3. Запускаем локальные проверки, PR CI и соответствующий installer/evidence
   gate.
4. После merge обновляем этот документ: статус, фактический результат и
   следующий gate.
5. Только затем создаём следующую ветку.

Если новая идея не помогает главному сценарию, не проходит privacy boundary или
не имеет понятного gate, она попадает в backlog, а не в текущую разработку.
